import React from "react";
import LoginForm from "../login/LoginForm";

const Login = () => {
  return (
    <section>
      <h1>Login</h1>
      <LoginForm />
    </section>
  );
};

export default Login;
